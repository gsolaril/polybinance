[BINANCE]
api_key = ...
secret = ...

[POLYMARKET]
# Wallet private key (64 hex chars, optional 0x prefix) — NOT the CLOB API key UUID
private_key = ...
# From Polymarket → Settings → API (or derive once via create_or_derive_api_creds)
api_key = ...
api_secret = ...
api_passphrase = ...
# Proxy/funder address shown in Polymarket (where USDC lives)
address = ...
# 0 = EOA, 1 = email/Magic proxy, 2 = Gnosis Safe
signature_type = 1
ctf = 0xE111180000d2663C0091e4f400237545B87B996B

[STRATEGY]
symbols = BTC ETH
polyevents = 5m
signature_type = 0

# Did you sign up / trade only via polymarket.com (browser)?
# ├─ Yes, and Settings shows a "Deposit" address
# │     → You need deposit wallet flow (type 3, funder = deposit address)
# │     → EOA-as-maker will keep failing with this error
# ├─ Yes, email/Magic login with a "Proxy" address
# │     → Type 1, funder = proxy address (not EOA)
# └─ Truly standalone MetaMask EOA with USDC only on that same address
#       → Type 0 might apply in theory, but v2 often still pushes deposit wallet
#         for API users — your error suggests you're NOT in that allowed bucket